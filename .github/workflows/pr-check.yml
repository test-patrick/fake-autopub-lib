name: PR Release Check

on:
  pull_request_target:
    branches: [main]

permissions:
  contents: read
  pull-requests: write

jobs:
  check-release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: "refs/pull/${{ github.event.number }}/merge"
          sparse-checkout: |
            RELEASE.md
            pyproject.toml
          sparse-checkout-cone-mode: false

      - uses: astral-sh/setup-uv@v7

      - name: Run autopub check
        env:
          GITHUB_TOKEN: ${{ github.token }}
          TYPEFULLY_API_KEY: ${{ secrets.TYPEFULLY_API_KEY }}
        run: |
          uvx --from autopub --prerelease=allow \
            --with pygithub \
            --with "strawberry-autopub-plugins @ git+https://github.com/strawberry-graphql/autopub-plugins.git@add-typefully-plugin" \
            autopub check
